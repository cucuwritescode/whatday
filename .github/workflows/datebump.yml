name: datebump

on:
  schedule:
    - cron: '0 0 * * *'
    - cron: '0 4 * * *'
    - cron: '0 8 * * *'
    - cron: '0 12 * * *'
    - cron: '0 16 * * *'
    - cron: '0 20 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: configure git identity
        run: |
          git config user.name "cucuwritescode"
          git config user.email "anicsounds@gmail.com"

      - name: compile
        run: clang -Wall -Wextra -O2 datebump.c -o datebump

      - name: run
        run: ./datebump
